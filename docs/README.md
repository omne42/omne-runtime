# omne-runtime 文档入口

`docs/` 是 `omne-runtime` 的 workspace 级记录系统入口。

如果你想快速判断一个能力该放在哪个 crate，按下面顺序读：

1. `docs/docs-system-map.md`
2. `docs/workspace-crate-boundaries.md`
3. `docs/source-layout.md`
4. `docs/quality-and-doc-maintenance.md`

这个目录不重复 crate 内部文档；更窄的事实应继续进入各 crate 自己的 `docs/`。
